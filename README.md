# [Your Name] — Portfolio

Personal portfolio site, built with plain HTML/CSS/JS and hosted on GitHub Pages.

Live at: https://it-is-rishabhsharma.github.io/

## Filling in content

Search the codebase for `[PLACEHOLDER` and `[YOUR` to find every spot that needs
real content — text, images, or links. See `index.html` for the full list, organized
by section (hero, about, work, testimonials, speaking, services, contact).

Images referenced under `images/` are placeholders — drop real files in with the
same names, or update the `src` attributes in `index.html`.

## Local preview

Just open `index.html` in a browser, or run a local static server:

```
npx serve .
```
